package test;

import model.Game;
import org.junit.Test;

public class GameTest {
    private Game game = new Game();

    // todo after Board's areShipsShot() function has been tested
    @Test
    public void testIsNotSolved() {
        //assertTrue(game.notSolved(game.getPlayer()));
    }
}
